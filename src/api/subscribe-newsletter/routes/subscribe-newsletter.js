'use strict';

/**
 * subscribe-newsletter router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subscribe-newsletter.subscribe-newsletter');
